#!/usr/bin/env node

/**
 * MCP Server Wrapper
 * 
 * This wrapper script provides the shebang and loads the actual MCP server.
 * The actual server logic is inlined from mcp-server-entry.ts during build.
 */

// The bundled server code will be inserted here during build
// This is just a placeholder - the actual build process creates the real bundled file